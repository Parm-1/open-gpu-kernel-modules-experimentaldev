# Experiment template

State the question, hypothesis, controlled variables, negative controls, procedure, and acceptance criteria. Never overwrite raw output. Record hashes in `artifacts.sha256` and classify the result in `verdict.md`.
