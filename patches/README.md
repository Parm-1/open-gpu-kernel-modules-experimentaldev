# Patch series

This directory is reserved for exported, reviewable patch series when a change is easier to validate against multiple NVIDIA snapshots without modifying the working tree directly.

The first planned series is:

```text
0001-nvkms-add-read-only-hdcp-state-query.patch
0002-nvidia-drm-add-experimental-hdcp-debug-report.patch
```

No patch may attach standard KMS content-protection properties until Gate 2 in `CHARTER.md` passes.
